from .db.models.fields import Fields

from typing import Any, Tuple, Dict, Type


def validate_body(body: Any, fields: List) -> Tuple[bool, Dict]:
    for field in fields:
        if not 

    return False, {}